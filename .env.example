# Convex development (the CLI writes the deployment values to .env.local).
CONVEX_AGENT_MODE=anonymous
CONVEX_DEPLOYMENT=
NEXT_PUBLIC_CONVEX_URL=
NEXT_PUBLIC_CONVEX_SITE_URL=

# Better Auth values are configured on the Convex deployment.
SITE_URL=http://localhost:3000
BETTER_AUTH_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
ADMIN_BOOTSTRAP_ROLE_MAP={"redactie@example.be":"admin"}

# Keystatic uses local files during development and GitHub mode in production.
KEYSTATIC_GITHUB_CLIENT_ID=
KEYSTATIC_GITHUB_CLIENT_SECRET=
KEYSTATIC_SECRET=
NEXT_PUBLIC_KEYSTATIC_GITHUB_APP_SLUG=
# Server-only fine-grained token used to read a selected Git branch in preview.
KEYSTATIC_GITHUB_READER_TOKEN=
# Comma-separated exact branches/prefixes accepted by signed preview sessions.
KEYSTATIC_PREVIEW_BRANCH_PREFIXES=master,main,content/
