# Neon schema docs (sandbox)

These files are seeded into the agent sandbox at `/workspace/docs/`.

**Status:** Phase B (Neon introspection) has not landed yet. The placeholder
describes the intended workflow; replace it with curated schema markdown once
the Neon secret and introspection pass are available.

Until then the agent must **not** invent table names, column names, or person ids.
If docs are still placeholders, say so in `researchSummary` and avoid fabricated facts.
